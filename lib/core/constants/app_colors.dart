import 'package:flutter/material.dart';

class AppColors {

  AppColors._();


  static const Color primaryDark = Color(0xFF0A0E17);    // Deep Midnight Blue background
  static const Color accentOrange = Color(0xFFFF7043);   // YatraPark signature Orange
  static const Color accentBlue = Color(0xFF2196F3);     // Scanning elements / Blue ring

  // Card & Input Elements
  static const Color surfaceDark = Color(0xFF161C2A);    // Container panels, text fields
  static const Color borderDark = Color(0xFF242F41);     // Subtle field borders

  // Typography
  static const Color textWhite = Color(0xFFFFFFFF);      // Primary text / Headings
  static const Color textMuted = Color(0xFFA0AABF);      // Secondary / Placeholders

  // Functional Status Colors
  static const Color successGreen = Color(0xFF4CAF50);   // "Active Session" badge / Green Entry Button
  static const Color errorRed = Color(0xFFF44336);       // Cancel action / Errors
}